# LOCOMO benchmark contract

All LOCOMO recall work uses the stable inserted `memory_id` as retrieval identity. Use no answer hints, no LLM judges, no answer-text scoring. Preserve frozen LOCOMO artifacts unless a new date-stamped full run is intentionally generated. Prove each recall or evaluation change with a focused failing test before broad validation.
